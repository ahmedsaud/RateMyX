.container
    .row
        .col-md-6.big-text
            strong
                | \#{{ category.name }}
    .row(ng-if="votes.length==0")
        .col-md-12
            h2.text-center There are not any votes in this category!
    .row
        .col-md-3.text-center(ng-repeat="vote in votes")
            .img-container
                a(href="#/vote/{{ vote._id }}")
                    img(ng-src="{{ vote.pictureUrl }}")