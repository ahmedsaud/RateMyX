.container
    .row
        div.col-md-6.big-text
            strong Top by most Likes
    .row
        .col-md-3.col-lg-3.col-sm-6.text-center(ng-repeat="vote in votesByMostLikes")
            .img-container
                a(href="#/vote/{{ vote._id }}")
                    img(ng-src="{{ vote.pictureUrl }}")