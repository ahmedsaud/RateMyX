.container
    row.col-md-3
    row.col-md-6
        table.table.text-center.table-hover.table-striped
            thead
                tr
                    th Name
                    th Options
            tbody
                tr(ng-repeat="category in categories")
                    td {{ category.name }}
                    td
                        button.btn.btn-xs.btn-danger(title="Remove", ng-really-message="Are you sure?" ng-really-click="removeCategory(category)")
                            | Delete
